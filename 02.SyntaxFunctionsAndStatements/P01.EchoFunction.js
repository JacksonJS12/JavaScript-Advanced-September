'use strict';

function echo(inputAsString) {
    let stringLength = inputAsString.length;

    console.log(stringLength);
    console.log(inputAsString);
}
let input = readline.question(string, callback());
echo(input);
